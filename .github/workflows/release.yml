name: Build and Push Docker Images

# DISABLED in the ORIN fork — superseded by .github/workflows/orin-docker.yml,
# which builds only the builder image and publishes it as
# ghcr.io/tanvir-hasan11/orin-builder:main. This upstream workflow builds five
# images and publishes them under the chatbotx-* names, which is unnecessary
# for ORIN and burned CI minutes on every push.

on:
  workflow_dispatch: {}
